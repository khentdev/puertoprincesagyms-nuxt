// this is dummy just for example
export type LoginParams = {
    email: string
    password: string
}

export type LoginDTO = {
    accessToken: string
    message: string
}

export type User = {
    id: string
    email: string
    name: string
}